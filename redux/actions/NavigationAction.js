const CartDropdownOpen = (isOpen) => {
  return {
    type: "IS_CART_DROPDOWN_OPEN",
    payload: isOpen,
  };
};

const WishlistDropdownOpen = (isOpen) => {
  return {
    type: "IS_CART_DROPDOWN_OPEN",
    payload: isOpen,
  };
};

const CategoriesDropdownOpen = (isOpen) => {
  return {
    type: "IS_CATEGORIES_DROPDOWN_OPEN",
    payload: isOpen,
  };
};
